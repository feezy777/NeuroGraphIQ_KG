---
name: session-2026-06-30
description: All work completed on 2026-06-30 — prompt engineering consolidation, pipeline fixes, dedup, dry run redesign, P0 features
metadata:
  type: session
---

## Completed Today

### 1. Prompt Engineering Consolidation
- Created `prompt_metadata.py` — unified 21 display names + metadata from 2 files
- Eliminated duplicate `EXTRACTION_PROMPT_DISPLAY_NAMES` (4 entries) vs `EXTRACTION_PROMPT_METADATA` (6 entries)
- Fixed `prompt_display_name()` to search both extraction and field-completion metadata
- Removed `list_extraction_prompt_template_items()` from `field_completion_prompt_engineering.py` → now in `prompt_metadata.py`

### 2. PoolExtractionModal 3-Step Wizard
- Step 1: Region selection (unchanged)
- Step 2: Model + params (ModelSelector, Temperature, MaxTokens)
- Step 3: Prompt template (full-height system/user textareas, edit toggle, schema warning)
- Locked panel height across steps 2-3 using `panelRef` + `lockedPanelHeight`

### 3. LLM Response Pipeline Audit + 6 Gap Fixes
- **P0**: Function/circuit extraction now retries on JSON parse failure (max 2 attempts)
- **P0**: Functions and circuits now have confidence-based merge (was skip-only)
- **P1**: Evidence records now SHA-256 dedup
- **P1**: Circuit dedup fallback from ORM relationship when payload_json is empty
- **P2**: Removed dead `allowed_connection_types` parameter
- **P2**: Narrowed `except Exception` to `(JSONDecodeError, ValueError, TypeError, KeyError)`

### 4. System-Wide Dedup Audit + 5 More Fixes
- Mirror triples: added 6-field canonical key dedup
- Mirror projection functions: confidence-based merge
- Mirror circuit functions: confidence-based merge + session_seen set
- Mirror circuit steps: same content at different step_order detection
- Mirror circuit projection memberships: 4-field canonical key
- Function dedup performance: pushed `func.lower()` to DB WHERE clause

### 5. P0: Extraction Fault Recovery
- `POST /composite-workflows/{id}/retry-failed` — re-runs only failed packs
- Frontend: "重试失败包 (N)" button on result screen

### 6. P0: Cost Visibility
- Token usage display in progress screen (用量 section)
- Cost estimate in result screen (费用 section)
- DeepSeek CN pricing: ¥1/1M input, ¥2/1M output
- Fixed: token estimates now flow through execution_summary to frontend

### 7. Dry Run Redesign
- Deleted 5 scattered dry run checkboxes from LlmExtractionPage.tsx
- Replaced PoolExtractionModal checkbox with radio mode selector (正式提取 / Dry Run 预览)
- Added optional sample pack execution (runs 1 pack through real LLM)
- Dry run now returns: pack plan, token estimates, cost, optional sample results
- Backend: `dry_run_sample_pack` flag, token estimation in connection extraction dry run path

### 8. MaxTokens Range Fix
- Default: 4096 → 16384
- Slider: 256-8192 → 512-65536
- Backend constraint: le=8192 → le=65536

**Why:** These were the highest-priority gaps identified through systematic auditing.
**How to apply:** All changes are committed on master. Resume with P1 items tomorrow.
